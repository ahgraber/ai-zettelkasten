# Implementation Plan: Docling Conversion Service

**Branch**: `001-docling-conversion-service` | **Date**: 2025-12-23 | **Spec**: [spec.md](./spec.md)\
**Input**: Feature specification from `/specs/001-docling-conversion-service/spec.md`

**Note**: This plan was generated by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

Convert KaraKeep bookmarks (HTML/PDF/arXiv/GitHub) to Markdown using Docling with durable state tracking, idempotency, and S3 artifact storage. Implement FastAPI REST API with SQLite job queue, 4 parallel Docling workers, exponential backoff retry logic, and HTML-only Web UI for operational monitoring. Support reprocessing with payload versioning and content-based deduplication via markdown hashing.

## Technical Context

**Language/Version**: Python 3.12+ (managed via uv)\
**Primary Dependencies**: FastAPI, Docling (v2.65+), SQLModel, boto3 (S3 client), xxhash, httpx, pydantic-settings\
**Storage**: SQLite with WAL mode via SQLModel; S3-compatible object storage for artifacts\
**Testing**: pytest with fixtures for deterministic transforms, contract tests for API, integration tests for S3 uploads\
**Target Platform**: Linux server (Docker/compose), macOS development (via Nix/uv devshell)\
**Project Type**: Single backend service (FastAPI REST API + background workers + HTML UI)\
**Performance Goals** (arbitrary, internal expectations):

- Single HTML bookmark (\<5MB): conversion complete within 120 seconds
- PDF bookmark (20 pages): conversion complete within 5 minutes
- 4 concurrent conversions without degradation
- API endpoints: \<100ms response for job submission/query
- Idempotency check: \<100ms for duplicate detection

**Constraints**:

- Fetch timeout: 30s (HTML), with retry backoff (3 attempts)
- Size caps: 100MB for HTML, 200MB for PDF
- Bounded concurrency: 4 parallel Docling workers (configurable)
- SQLite single-writer pattern with lock retry logic
- Memory: ~8GB RAM minimum for 4 concurrent Docling processes
- Temp workspace: 10GB minimum disk space

**Scale/Scope**:

- Internal-only deployment (no internet-facing auth required)
- Expected throughput: hundreds of bookmarks per day
- 4 parallel conversion workers
- Job history retention: retain all aside from user-directed archive/purge strategy for old jobs via the HTML webui
- S3 bucket quota: assume unlimited for initial deployment
- **Bookmark Metadata**: Bookmarks track two independent dimensions: `content_type` (html/pdf—format of content from KaraKeep) and `source_type` (arxiv/github/other—origin parsed from URL). Enables flexible handling: arXiv bookmarks can be PDF or HTML; GitHub bookmarks are always HTML; generic sources may be either format.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **G1 Data provenance**:

  - Sources: KaraKeep bookmarks (via karakeep_id, url), arXiv (via arxiv_id in URL), GitHub (via owner/repo in URL)
  - Metadata: bookmarks table stores karakeep_id, url, normalized_url, source_type, title, aizk_uuid
  - Hashing: xxhash64 of normalized Markdown content (markdown_hash_xx64) for deduplication and change detection
  - Raw inputs: source HTML/PDF retained only temporarily in fetch stage; conversion_outputs.manifest_key references all generated artifacts
  - Replay: conversion_outputs records docling_version, pipeline_name, payload_version, config_hash enabling re-conversion with same parameters
  - **Status**: ✅ PASS

- **G2 Reproducibility**:

  - Pinned dependencies: Docling v2.65+ pinned in pyproject.toml; uv lock ensures deterministic resolution
  - Environment: Runs in Nix/uv devshell; dependencies declared in pyproject.toml
  - Idempotency: idempotency_key computed from hash(aizk_uuid + payload_version + docling_version + config_hash); duplicate submissions return existing job without re-processing
  - Seeds: Docling pipelines are deterministic given same inputs; no random seeds required
  - Side effects: Job submission checks idempotency_key before creating new record; S3 uploads happen only after job completion with atomic status update in transaction
  - **Status**: ✅ PASS

- **G3 Test-first**:

  - Contract tests: API endpoint schemas (POST /v1/jobs, GET /v1/jobs/{id}, GET /v1/outputs/{uuid}, POST /v1/jobs/batch, POST /v1/jobs/actions) tested against OpenAPI spec
  - Fixture tests: Deterministic Markdown normalization, URL normalization, idempotency_key computation, markdown_hash_xx64 computation with known inputs/outputs
  - Integration tests: S3 upload verification with local S3-compatible test instance, SQLite transaction isolation, job status transitions, retry logic
  - Regression tests: Edge cases from spec (404s, empty Markdown, duplicate idempotency_key, S3 failures) covered before merge
  - **Status**: ✅ PASS (tests to be added during implementation phase per TDD)

- **G4 Privacy & safety**:

  - No PII: Bookmarks contain only URLs, titles, identifiers (no user authentication or personal data); content is from public internet so PII is not expected
  - Redaction: Not applicable (public web content only)
  - Access controls: Internal-only deployment behind localhost/VPN; no internet-facing endpoints
  - External APIs: Fetches content from any URL specified in bookmarks (unrestricted); primary sources include arXiv, GitHub, and general web content
  - Secrets: S3 credentials, API keys loaded from environment variables via pydantic-settings; no secrets in repo or logs
  - **Status**: ✅ PASS

- **G5 Observability & versioning**:

  - Structured logging: All processing events include aizk_uuid, job_id, karakeep_id, status as context fields using Python logging with structured formatters
  - Metrics: Emit queue depth, job duration histogram, job status counts (SUCCEEDED/FAILED/CANCELLED), fetch latency, S3 upload latency
  - Versioning: conversion_outputs records payload_version, docling_version; manifest.json includes artifact list and metadata
  - Migration: Payload version bumps require migration note in CHANGELOG.md; markdown_hash_xx64 enables detection of meaningful content changes
  - **Status**: ✅ PASS

- **G6 ADRs**:

  - ADR-001: S3 storage strategy and atomic finalization using database transactions (documented in research.md)
  - ADR-002: Idempotency and payload_version semantics for reprocessing (documented in research.md)
  - ADR-003: SQLite with WAL Mode for Initial Deployment (documented in research.md)
  - **Status**: ✅ PASS (ADRs documented in research.md)

- **G7 Governance**:

  - Changelog: CHANGELOG.md will be updated following keepachangelog.com format (Added/Changed/Fixed sections)
  - Semantic Versioning: Feature release will bump MINOR version (0.0.1 → 0.1.0)
  - Conventional Commits: PR commits/title will use `feat(conversion):` prefix
  - **Status**: ✅ PASS

- **G8 Preconditions**:

  - spec.md: Complete and approved (provided in spec.md attachment)
  - plan.md: Being generated by this command (will be reviewed after completion)
  - **Status**: ✅ PASS

## Project Structure

### Documentation (this feature)

```text
specs/001-docling-conversion-service/
├── spec.md              # Feature specification (input)
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output: research decisions and ADRs
├── data-model.md        # Phase 1 output: database schema and entities
├── quickstart.md        # Phase 1 output: developer setup guide
├── contracts/           # Phase 1 output: API contracts
│   └── openapi.yaml     # OpenAPI 3.1 specification
└── tasks.md             # Phase 2 output (/speckit.tasks command)
```

### Source Code (repository root)

```text
src/aizk/datamodel/           # Shared SQLModel database models (used by all features)
├── __init__.py               # Imports all models to populate SQLModel.metadata
├── bookmark.py               # Bookmark entity
├── job.py                    # ConversionJob entity
└── output.py                 # ConversionOutput entity

src/aizk/db.py                # Shared DB utilities (engine/session/PRAGMAs/create_db)

src/aizk/conversion/          # Conversion service feature package
├── __init__.py
├── api/                      # FastAPI application
│   ├── __init__.py
│   ├── main.py               # FastAPI app setup, lifespan, middleware
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── jobs.py           # Job submission, query, retry, cancel
│   │   ├── outputs.py        # Output retrieval
│   │   └── ui.py             # Web UI endpoints (HTML templates)
│   ├── schemas/              # Pydantic request/response schemas
│   │   ├── __init__.py
│   │   ├── jobs.py
│   │   └── outputs.py
│   └── dependencies.py       # Dependency injection (DB session, S3 client)
├── workers/                  # Background conversion workers
│   ├── __init__.py
│   ├── worker.py             # Worker main loop, job pickup, status updates
│   ├── fetcher.py            # URL fetching with retry and size limits
│   └── converter.py          # Docling pipeline execution
├── storage/                  # S3 storage operations
│   ├── __init__.py
│   ├── s3_client.py          # S3 upload, download, verification
│   └── manifest.py           # Manifest.json generation
├── utilities/                # Shared utilities
│   ├── __init__.py
│   ├── url_utils.py          # URL normalization, source type detection
│   ├── hashing.py            # Idempotency key, Markdown hash computation
│   ├── filename_utils.py     # Filename normalization
│   └── config.py             # Configuration loading (pydantic-settings)
├── templates/                # Jinja2 HTML templates for Web UI
│   ├── base.html
│   └── jobs.html
├── cli.py                    # CLI entrypoint (db init, serve, worker)
│   └── README.md
└── migrations/               # Alembic migrations (future use)

tests/conversion/             # Test suite for conversion service
├── __init__.py
├── conftest.py               # Pytest fixtures (test DB, S3 client, API client)
├── unit/                     # Unit tests (no external dependencies)
│   ├── test_url_utils.py
│   ├── test_hashing.py
│   ├── test_filename_utils.py
│   └── test_models.py
├── integration/              # Integration tests (requires S3, DB)
│   ├── test_api_jobs.py
│   ├── test_api_outputs.py
│   ├── test_worker.py
│   └── test_s3_storage.py
└── contract/                 # Contract tests (OpenAPI validation)
    └── test_openapi_compliance.py
```

**Structure Decision**: Single backend project structure selected. This is a standalone service (not web app or mobile) providing REST API and background workers. Conversion service is self-contained within `src/aizk/conversion/` package to maintain clean separation from existing AIZK codebase components (ai/, datamodel/, utilities/). Tests mirror source structure under `tests/conversion/`.

## Complexity Tracking

All Constitution Check gates passed without violations requiring justification. No additional complexity tracking needed.

**Gate Summary**:

- ✅ G1: Data provenance fully documented
- ✅ G2: Reproducibility via uv/Nix, pinned dependencies, idempotency
- ✅ G3: Test-first approach planned (contract/fixture/integration tests)
- ✅ G4: Privacy & safety: no PII, internal-only deployment, secrets in env vars
- ✅ G5: Observability via structured logs and metrics
- ✅ G6: ADRs documented in research.md (sufficient for implementation)
- ✅ G7: Governance compliance (CHANGELOG, semver, conventional commits)
- ✅ G8: Preconditions met (spec.md approved, plan.md complete)

No complexity violations to justify.

---

## Implementation Readiness

**Phase 0 (Research)**: ✅ Complete

- All NEEDS CLARIFICATION items resolved in research.md
- Three ADRs documented: S3 storage, idempotency semantics, SQLite WAL mode
- Best practices researched: Docling configuration, arXiv/GitHub fetching, filename normalization

**Phase 1 (Design)**: ✅ Complete

- Data model defined in data-model.md with full SQLModel schemas
- API contracts defined in contracts/openapi.yaml
- Developer quickstart guide created in quickstart.md
- Agent context updated (.github/agents/copilot-instructions.md)

**Phase 2 (Tasks)**: ⏳ Ready to generate

- Run `/speckit.tasks` command to break down implementation into atomic tasks
- Tasks will reference data-model.md and contracts/openapi.yaml for implementation details

**Constitution Re-Check Post-Design**: ✅ Passed

- All gates remain compliant after design phase
- ADRs documented in research.md (sufficient for implementation)
- No deviations from constitution principles

---

## Next Steps

1. **Generate tasks**: Run `/speckit.tasks` command to create tasks.md with implementation breakdown

2. **Implementation**: Execute tasks following TDD approach

   - Write tests first per constitution G3
   - Implement in order: models → storage → workers → API → UI
   - Update CHANGELOG.md with `feat(conversion):` entries

3. **Review & Merge**:

   - Ensure all constitution gates verified in PR
   - Update version 0.0.1 → 0.1.0 (MINOR bump for new feature)
   - Merge to main after approval

---

**Planning Complete**: 2025-12-23\
**Branch**: 001-docling-conversion-service\
**Artifacts**:

- ✅ plan.md (this file)
- ✅ research.md
- ✅ data-model.md
- ✅ contracts/openapi.yaml
- ✅ quickstart.md
- ⏳ tasks.md (generate with `/speckit.tasks`)
